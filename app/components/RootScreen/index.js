import React from 'react'
import RootScreen from './component'

const RootScreenContainer = ({ navigation }) => {
  return (
    <RootScreen navigation={navigation} />
  )
}

export default RootScreenContainer
